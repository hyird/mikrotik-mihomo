version: "3.8"

services:
  mihomo:
    image: ghcr.io/hyird/mikrotik-mihomo:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MIHOMO_VERSION: v1.19.27
        METACUBEXD_REF: 640e8cbc3472bfc913bfc3296df1f1fda0d607f3
    container_name: mihomo
    hostname: mihomo

    network_mode: host

    privileged: true
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
      - NET_RAW
      - SYS_MODULE

    volumes:
      - ./clash:/etc/config/clash
      - ./logs:/tmp/ppgw
      - /sys:/sys:ro
      - /proc:/proc:ro

    environment:
      - FAKE_CIDR=198.18.0.0/16
      - TPROXY_PORT=1082
      - BLOCK_QUIC=true
      - CLASH_WEB_PORT=80
      - CLASH_WEB_PASSWORD=clashpass
      - DEFAULT_BACKEND_URL=window.location.origin
      - SLEEPTIME=30
      - SUBURL=https://example.com/subscribe
      - TZ=Asia/Shanghai

    restart: unless-stopped
